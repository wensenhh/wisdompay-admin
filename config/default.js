const config = {
  // 启动端口
  port: 3000,

  // 数据库配置
  database: {
    DATABASE: 'data_stat',
    USERNAME: 'root',
    PASSWORD: 'root',
    PORT: '3306',
    HOST: '127.0.0.1'
  },
 
}

module.exports = config
